@model IEnumerable<kpurganaa.Models.Habitacione>

@{
    ViewData["Title"] = "Index";
}
<div class="vh-100">
    <div>
        <h1 class="text-center">Index</h1>

        <p class="text-center">
            <a asp-action="Create" class="bi bi-file-plus btn btn-success">Create New</a>
        </p>
    </div>
    <table class="table table-striped table-sm table-hover">
        <thead class=" table-dark">
            <tr>
                <th>
                    @Html.DisplayNameFor(model => model.NorHabitacion)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.Descripcion)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.EstadoHabitacion)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.PrecioHabitacion)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.IdTipoHabitacionNavigation)
                </th>
                <th></th>
            </tr>
        </thead>
        <tbody class="table">
    @foreach (var item in Model) {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.NorHabitacion)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Descripcion)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.EstadoHabitacion)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.PrecioHabitacion)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.IdTipoHabitacionNavigation.IdTipoHabitacion)
                </td>
                <td>
                    <div>                        <a asp-action="Edit" asp-route-id="@item.IdHabitacion" class="bi bi-pen  btn btn-primary btn-warning text-black"></a>
                        <a asp-action="Details" asp-route-id="@item.IdHabitacion" class="bi bi-file-earmark-zip  btn btn-primary text-black"></a> 
                        <a asp-action="Delete" asp-route-id="@item.IdHabitacion" class="bi bi-archive  btn btn-danger text-black"></a>
                    </div>
                </td>
            </tr>
    }
        </tbody>
    </table>
</div>
